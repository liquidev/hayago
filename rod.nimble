# Package

version       = "0.1.0"
author        = "liquid600pgm"
description   = "An embeddable scripting language for Nim"
license       = "MIT"
srcDir        = "src"

# Dependencies

requires "nim >= 1.2.0"
